# Blender integration

Target behavior:

```text
GLB source
→ normalized bounding box
→ ViewContract cameras
→ RGB / alpha / depth / normal / camera.json
→ dataset manifest
```

The actual turntable renderer is planned for Phase 2.

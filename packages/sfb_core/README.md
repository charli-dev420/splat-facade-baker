# sfb-core

Core Splat Facade Baker package.

Current stable path:

```text
albedo + alpha + depth
→ cleanup
→ depth-card + LODs
→ SFB package
```

Install:

```bash
pip install -e packages/sfb_core[dev]
```

Run:

```bash
sfb bake-maps --help
```
